# Асинхронный JavaScript

Асинхронность позволяет выполнять долгие операции (запросы к серверу, таймеры) без блокировки основного потока.

## Колбэки и промисы

Старый способ:
```javascript
setTimeout(() => console.log('Через секунду'), 1000);