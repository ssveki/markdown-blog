# Работа с DOM

DOM (Document Object Model) - это представление HTML-страницы в виде дерева узлов. JavaScript позволяет изменять его динамически.

## Поиск элементов

```javascript
const title = document.getElementById('main-title');
const items = document.querySelectorAll('.list-item');